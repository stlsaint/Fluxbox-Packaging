#!/bin/sh
set -e

Fluxbox_entry="/usr/bin/fluxbox"

case "$1" in
    purge)
        test -d /etc/X11/fluxbox && rm -rf /etc/X11/fluxbox
	;;
    remove)
        ;;
    *)
        ;;
esac


#DEBHELPER#
